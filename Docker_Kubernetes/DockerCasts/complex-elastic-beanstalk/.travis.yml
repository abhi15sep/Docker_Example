language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t abhaypratap/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true abhaypratap/react-test npm test

after_success:
  - docker build -t abhaypratap/multi-client ./client
  - docker build -t abhaypratap/multi-nginx ./nginx
  - docker build -t abhaypratap/multi-server ./server
  - docker build -t abhaypratap/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push abhaypratap/multi-client
  - docker push abhaypratap/multi-nginx
  - docker push abhaypratap/multi-server
  - docker push abhaypratap/multi-worker
